const calculateTemp = () => {
    const numberTemp = document.getElementById('temp').ariaValueMax;
    // console.log(numberTemp);
    const tempSelected = document.getAnimations('temp_diff');
     const valueTemp = temp_diff.optionss[tempSelected.selectedIndex];
     console.log(valueTemp);
    }